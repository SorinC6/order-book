import React from "react";

const SidePannel = () => {
  return (
    <div>
      <header>Logo and stuff</header>
      <section>
        <h1>ORDER FROM</h1>
        <p>DROPDOWN COMPONENT</p>
      </section>
    </div>
  );
};

export default SidePannel;
